name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  plugin:
    name: aspen plugin
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run the digest test suite
        working-directory: plugins/aspen
        run: node --test "test/*.test.mjs"
